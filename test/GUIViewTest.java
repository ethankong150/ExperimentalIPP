/**
 * Class to test the GUIView class and ensure the GUI is properly displaying to the user.
 */
public class GUIViewTest {

  //@Test
  public void testGUIView() {
    // didn't have time to implement, but the plan was to try to createGraphics for a
    // BufferedImage and copy the Graphics from the view to the Graphics of the BufferedImage.
    // We'd also createGraphics for another BufferedImage and we'd manually draw the expected
    // output onto that Graphics. Then we'd compare the two BufferedImages by converting them
    // to Images from the model package.
    // This would probably take multiple tests (at least one for Histogram, at least one for
    // the image, etc.)
  }
}
